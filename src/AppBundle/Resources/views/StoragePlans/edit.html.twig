{% extends '::baseadm.html.twig' %}

{% block body -%}
    <h5>StoragePlans edit</h5>

    {{ form(edit_form,{ 'attr': {'class': 'formAmd'} }) }}

    <ul class="record_actions supe">
    <li>
        <a href="{{ path('storageplans') }}" class="submit btnAdm lilaOscuro">
            volver
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
